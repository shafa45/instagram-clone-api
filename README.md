# Instagram FastAPI

This is a FastAPI implementation of an Instagram-like social media platform. It allows users to create an account, create a post, comment on post.

## Installation

1. Clone the repository:


git clone https://github.com/yourusername/instagram-fastapi.git


2. Install the required dependencies:


pip install -r requirements.txt


3. Start the server:


uvicorn main:app --reload


4. Navigate to `http://localhost:8000/docs` in your web browser to view the API documentation and test the API.
